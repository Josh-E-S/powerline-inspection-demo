line-length = 79
target-version = "py310"  # app must run on whatever HF Spaces provides

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]  # long lines in docstrings/URLs are intentional

[lint.per-file-ignores]
"tests/*" = ["E402"]  # sys.path setup must precede imports
